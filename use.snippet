	// 编码服务
	/**
	 * ${0:变量注释}
	 */
	const r$CURRENT_SECONDS_UNIX = await(() => {
		const p1 = '$1';	// 编码名称，也为前缀
		const p2 = $2;	// 需要生成编码的个数，必须为大于0的整数
		const p3 = $3;	// 编码中数字的位数
		return an15(p1, p2, p3);
	})();